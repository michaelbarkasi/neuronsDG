% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neurons.R
\name{estimate.autocorr.params}
\alias{estimate.autocorr.params}
\title{Function to estimate autocorrelation parameters using dichotomized Gaussian simulations}
\usage{
estimate.autocorr.params(
  neuron_list,
  n_trials_per_sim = 300,
  n_sims_per_neurons = 100,
  bin_count_action = "sum",
  A0 = 0.001,
  tau0 = 1,
  ctol = 1e-08,
  max_evals = 500,
  verbose = FALSE
)
}
\arguments{
\item{neuron_list}{An R list of neuron objects.}

\item{n_trials_per_sim}{Number of trials to simulate for each simulation (default: 300).}

\item{n_sims_per_neurons}{Number of simulations to run for each neuron (default: 100).}

\item{bin_count_action}{Method for counting spikes in each bin when computing autocorrelation; one of "boolean", "mean", or "sum" (default: "sum").}

\item{A0}{Initial guess for amplitude parameter of exponential decay function (default: 0.001).}

\item{tau0}{Initial guess for time constant parameter of exponential decay function (default: 1.0).}

\item{ctol}{Convergence tolerance for fitting exponential decay function (default: 1e-8).}

\item{max_evals}{Maximum number of evaluations for fitting exponential decay function (default: 500).}

\item{verbose}{Logical indicating whether to print progress messages (default: FALSE).}
}
\value{
A list containing a data frame of autocorrelation parameter estimates (one row per simulation), a data frame of neuron identifiers (one row per neuron), and the number of simulations run per neuron.
}
\description{
This function performs the same estimate-and-fit procedure as \code{process.autocorr}, but does so multiple times for each neuron using simulated spike rasters generated from a dichotomized Gaussian model of that neuron.
}
